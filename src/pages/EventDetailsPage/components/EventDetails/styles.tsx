import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
  title: {
    wordWrap: 'break-word',
  },
  hosts: {
    wordWrap: 'break-word',
  },
  hostName: {
    marginBottom: '5px',
  },
  location: {
    wordWrap: 'break-word',
    maxWidth: 240,
  },
});

export default useStyles;
